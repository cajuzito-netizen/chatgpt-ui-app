/**
 * Product identity for this shell instance.
 * Fork this starter and change these values per app (email sender, CRM, etc.).
 */
export const APP_NAME = 'YourApp'
export const APP_TAGLINE = 'Generic app shell — build your product here.'
